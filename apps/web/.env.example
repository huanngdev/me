# GitHub (existing)
GITHUB_TOKEN=

# Postgres (Drizzle ORM)
DATABASE_URL=postgres://user:password@host:5432/dbname

# Redis (TCP connection string — ioredis)
REDIS_URL=redis://default:password@host:6379

# 32+ random bytes. Generate with: openssl rand -hex 32
VIEW_HASH_SALT=
